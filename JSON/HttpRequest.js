//XML HttpRequest
//All modern browsers have a built-in XMLHttpRequest object to request
//data from the server.

//The XMLHttpRequest object is a developers dream, because you can:

// Update a web page without reloading the page
// Request data from a server - after the page has loaded
// Receive data from a server  - after the page has loaded
// Send data to a server - in the background

//This is a get request from json file.
//You can also use a library like axios or JQuery
//if you dont wanna use an external library you can just use JS w the syntax below

// XMLHttp request is an object from your browser
// var xhttp = new XMLHttpRequest();
// xhttp.onreadystatechange = function() {
//   if (this.readyState == 4 && this.status == 200) {
//     // Typical action to be performed when the document is ready:
//     document.getElementById("demo").innerHTML = xhttp.responseText;
//   }
// };
// xhttp.open("GET", "filename", true);
// xhttp.send();

//adding changes here as a git test