const numbers = [1, 2, 3];
const joined = numbers.join(','); //separator is an optional argument that will appear between the elements of the array
console.log(joined);

const message = 'This is my first message';
const parts = message.split(' ');
console.log(parts);